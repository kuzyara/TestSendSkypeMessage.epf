
Процедура КнопкаВыполнитьНажатие(Кнопка)
	Токен = ПолучитьТокенSkype(IDПриложения, Пароль);
	IDЧата_ = IDЧата;
	ТекстСообщения_ = ТекстСообщения;
	ОтправитьСообщениеSkype(Токен, IDЧата_, ТекстСообщения_);
КонецПроцедуры
